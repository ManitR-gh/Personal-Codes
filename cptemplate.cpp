#include <bits/stdc++.h>
using namespace std;

// Shortcuts
#define ll long long
#define pb push_back
#define all(x) (x).begin(), (x).end()
#define fast_io ios::sync_with_stdio(false); cin.tie(nullptr);

class Solution {
public:
    void solve() {
        // Write your solution logic here
    }
};

int main() {
    fast_io;

#ifndef ONLINE_JUDGE
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
#endif

    int t = 1; // number of test cases
    // Uncomment below line if multiple test cases are given:
    // cin >> t;

    Solution obj;
    while (t--) {
        obj.solve();
    }

    return 0;
}
